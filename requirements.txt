gunicorn==21.2.0
flask>=2.3
numpy>=1.24
scipy>=1.10
soundfile>=0.12
matplotlib>=3.7
Pillow>=10.0
batdetect2>=1.3
torch>=2.0
torchaudio>=2.0
# Linux only — Tadarida-D bat detector (optional; falls back gracefully on macOS/Windows)
pytadarida
